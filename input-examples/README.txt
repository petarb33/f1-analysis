In order to run each script, copy .json file for the given script, paste it
into script folder, and rename it jo "session.json".
This is done because each script requires different type of input.
Change the input values as you like.